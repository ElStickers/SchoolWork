SList* list2 = new SList; //and SList list1 are the same;

dynamicaly allocated dont call their desctructor automatically you need the delete operator to do so
with statically allocated it gets called automatically
