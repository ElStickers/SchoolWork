#include <iostream>
#include <string>
using std::cout;
using std::endl;
using std::string;

unsigned int hashBySize(string s) {
  return s.size();
}

int int main(int argc, char const *argv[]) {
  string test[] = {"apple", "orange", "banana", " "
  return 0;
}
