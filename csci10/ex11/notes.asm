working with arrays on x86
access a specific element
  #byte per element:
    scale
    offset
    base
      [base+offset*scale]

locating min/max values in an array
  greddy algorithms
    assume the first value is the largest
    prove yourself wrong (or not)
///////////////////ELIXIR?///////////////////////
